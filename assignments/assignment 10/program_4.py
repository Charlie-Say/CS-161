'''
import filter_even() and give it an alias and test
filter_even() using a list with random numbers and words as elements.
'''

from mytools import filter_even as fe

test = fe(['apple', 2, 31, 5, 40, 'dog', 'cat', 7, 22, 9])

print(test)